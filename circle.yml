dependencies:
  pre:
#    - wget https://github.com/gohugoio/hugo/releases/download/v0.46/hugo_0.46_Linux-64bit.deb
#    - sudo dpkg -i hugo*.deb
#    - npm install -g aerobatic-cli

deployment:
  master:
    branch: development
    commands:
#      - git clone https://github.com/qredo/Qredo-Documentation.git
#      - hugo --baseURL https://!!baseurl!! --buildDrafts
#      - aero deploy -d public